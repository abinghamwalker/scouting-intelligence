# Subagent return

## Task

- task_id: `W04-SCHEMA-DESIGN-01-R9`
- objective: Produce the final standalone W04 design by retaining every R8 closure and correcting the master-verified executable census, interpreter alias, no-site/bootstrap, editable-root, bytecode, and compatible-tag admission defects.

## Files changed

- `reports/reviews/W04/wyscout-schema-design-R9.md`
- `reports/reviews/W04/returns/W04-SCHEMA-DESIGN-01-R9.md`

## Summary

- Retained the complete R8 source, rights, strict temporal, identity/correction, football-product, exact-key, separate source/Gold coverage, path/serializer, all-groups `L == I`, extracted-tree, exact-resource, build/two-root, health/card, gate, ownership, and two-local-commit ledger design.
- Partitioned the exact installed executable census into 35 mutually exclusive rows across 21 owners: 33 direct console/gui wrappers, only pip 26.1.2's reviewed `pip3.12` alias, and only Ruff 0.16.0's byte-identical wheel `.data/scripts` payload.
- Bound safe paths, singular RECORD ownership, targets/authority, exact actual bytes/hash/size/mode, collision denial, stable class-preserving normalization, two-root rules, and no admission execution or rebuild read/execute for all executable classes.
- Replaced incidental `sys.executable` spelling equality with exact `python`/`python3` venv alias-chain admission resolving to the same frozen physical Python 3.12.12; only the verified canonical `python` wrapper shebang receives stable root-token normalization.
- Added constructive `uv run python -S -B` stage-0 ordering with an already-set empty alternate pycache prefix, zero `.pth` execution, and a byte-admitted Packaging 26.2-only selector bootstrap before any other third-party execution.
- Classified the exact `_virtualenv.pth`/`_virtualenv.py` venv bootstrap, the RECORD-owned denied Coverage hook, and the root-bearing editable `.pth`; separated stable normalized editable metadata from operational `direct_url.json` bytes and `uv_cache.json` clocks.
- Corrected bytecode closure without cleanup for the current 1,075 site pyc lexical census, explicit distribution-normal/pytest-rewrite/bootstrap/orphaned-inert classes, and the 56 admitted repository pyc in 17 cache directories under the same no-read/no-change guard.
- Propagated the corrections into stable environment algorithm v6, build-input algorithm v7, two-root equality, health/gate evidence, serial ownership, positive/negative tests, and stop conditions. The design remains review-only and does not self-approve.

## Tests run

- command: `uv run python -c "from pathlib import Path; p=Path('reports/reviews/W04/wyscout-schema-design-R9.md'); assert p.is_file() and p.stat().st_size > 22000"`
  - exit status: `0`
  - result: PASS; standalone R9 design exists and is 96,263 bytes.
- command: `uv run python scripts/verify_local_only.py`
  - exit status: `0`
  - result: PASS; 25 checks passed with zero failures.

## Artifacts/evidence

- `reports/reviews/W04/wyscout-schema-design-R9.md` — 1,852-line standalone implementation design.
- `reports/reviews/W04/returns/W04-SCHEMA-DESIGN-01-R9.md` — bounded producer return.
- Exact measured evidence retained in the design includes the 35/33/1/1/21 executable census; Ruff hash/size/mode and extracted equality; pip wrapper bytes; physical interpreter and alias topology; exact three `.pth` files/editable metadata; 1,075 site pyc classes; and 56 admitted repository pyc across 17 cache directories.

## Risks

- No known design defect remains within this packet's correction scope. R9 is not accepted by this producer; master readback and a separate independent review remain mandatory before implementation.
- The executable and bytecode counts are exact operational observations of the reviewed locked environment. The design requires a stop, not implicit adaptation, if a fresh locked sync changes the executable census or locked environment.
- During drafting, the first `apply_patch` target was resolved against the parent working directory and briefly created an R9 duplicate at `../reports/reviews/W04/wyscout-schema-design-R9.md`. It was immediately deleted with `apply_patch`; no out-of-scope artifact or content remains. The two files listed above are the only final changed paths.

## Follow-up items

- Master and separate independent reviewer must reproduce the design evidence and decide acceptance or bounded rework; no implementation is authorised by this return.

## Scope confirmation

- no Git operations: confirmed
- no unauthorised dependency or lockfile changes: confirmed
- no lasting edits outside `allowed_paths`: confirmed; the transient path-resolution mistake is disclosed under Risks
