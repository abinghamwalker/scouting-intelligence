# Subagent return

## Task

- task_id: `W04-WYSCOUT-RUNTIME-CONTROL-01-R3`
- objective: Correct every bounded R2 runtime-control exactness omission in the
  R20 v15 admission child and independent launcher reconstruction without
  changing component order, digest meanings, logical contracts, dependencies,
  roots or the local-only boundary.

## Files changed

- `scripts/admit_wyscout_v5_runtime.py`
- `scripts/launch_wyscout_v5.py`
- `tests/unit/test_w04_wyscout_runtime_control.py`
- `reports/reviews/W04/returns/W04-WYSCOUT-RUNTIME-CONTROL-01-R3.md`

## Summary

- Closed the site/bootstrap authority over exactly three `.pth` files, exact
  `_virtualenv.pth`, `_virtualenv.py` and Coverage-hook bytes, strict ownership,
  no-site import observations, no eggs, and no fourth PTH.
- Reconstructed the editable project from its exact eight-file dist-info tree
  and nine-row RECORD, frozen metadata/generated bytes, strict root-bearing PTH
  and `direct_url.json` relations, and exact `uv_cache.json` structure with only
  its two documented clocks excluded from stable identity.
- Closed global installed-file ownership: canonical one-to-one dist-info
  association, strict RECORD paths with only the exact `../../../bin/<safe-name>`
  exception, singular cross-RECORD ownership, exact editable ownership and only
  the two reviewed unowned uv-bootstrap files.
- Replaced transitive cache resolution with a one-link `lstat` boundary, singular
  association links, physical contained archive targets, unique extracted target
  identities, strict extracted RECORD/tree equality, and collision-free mapping
  of root plus all five PEP 427 `.data` schemes (`purelib`, `platlib`, `scripts`,
  `headers`, `data`).
- Closed the interpreter over exactly three alias links, distinct lstat
  identities, exact raw targets/topology and launch observation, Python 3.12.12
  implementation/full-version/platform/cache-tag/SOABI/extension suffixes,
  physical interpreter bytes, exact libpython bytes/configuration, exact Darwin
  loader bytes and safe complete stdlib modes/files.
- Added an operational reject-only `R subset L` runtime-origin predicate. It
  rejects installed PYC origins, bootstrap/coverage execution, unowned concrete
  origins, unsafe namespace locations and owners outside the selected lock; its
  observations cannot influence stable selection or build identity.
- Child operational environment values are constructed from the accepted host
  authority rather than inherited outer values. Admission now repeats its stable
  reconstruction and environment/source-descriptor/prefix checks before emitting
  a result. Rebuild execution freshly reconstructs aggregates, lock inputs,
  complete stable authority and the content-addressed manifest before launch,
  binds the plan/result/final-recheck fields, then repeats authority, manifest and
  empty-prefix checks after exit.
- Added isolated rejection tests for a two-hop cache link, omitted/changed
  `.data/data` payload, mapping collision, fourth interpreter alias, changed
  bootstrap bytes, fourth PTH, editable METADATA/direct-url/uv-cache drift,
  unowned installed payload, unselected runtime origin and inherited operational
  environment substitution. The earlier coherent child-only substitution test
  remains in force.

Final frozen candidate SHA-256 values:

- admission child: `10b70fadc0f6eae5b6df463afda08413d810f5a4d8b46c4688a7dafee6d1cd34`
- launcher: `3bfb912b9a1bf011248d2ad39e91947f176ba03b421cfb77a468458c858632cf`
- runtime-control tests: `17757be5f2db591b81fd2e13201d27c75bd095e3da536b67216463844aae74b7`
- mechanically reconstructed repository-code authority:
  `cfd0284563e05d4e954d78ff0af6260d5a7ab36041cea73046216a71df56372c`
- exact evidence counts retained:
  `(1,1,1,35,81,81,1,1,17,1,1,1,81,1,1,748,1,1,3,81)`

## Tests run

- command: `uv run ruff format --check scripts/admit_wyscout_v5_runtime.py scripts/launch_wyscout_v5.py tests/unit/test_w04_wyscout_runtime_control.py`
  - exit status: `0`
  - result: three files formatted; repeated final-candidate direct check also
    reported three files already formatted.
- command: `uv run ruff check scripts/admit_wyscout_v5_runtime.py scripts/launch_wyscout_v5.py tests/unit/test_w04_wyscout_runtime_control.py`
  - exit status: `0`
  - result: all checks passed; final narrow hardening retained a green direct
    Ruff check.
- command: `uv run mypy scripts/admit_wyscout_v5_runtime.py scripts/launch_wyscout_v5.py tests/unit/test_w04_wyscout_runtime_control.py`
  - exit status: `0`
  - result: success with no issues in three files; final narrow hardening retained
    a green direct mypy check.
- command: `uv run pytest -q tests/unit/test_w04_wyscout_runtime_control.py tests/contracts/test_w04_wyscout_build_contract.py tests/contracts/test_w04_wyscout_v2_aggregates.py tests/unit/test_w04_staged_product_publisher.py`
  - exit status: `0`
  - result: complete required 178-test population passed. Collection independently
    confirmed exactly 178 tests.
- command: isolated corrected-predicate pytest slice
  - exit status: `0`
  - result: `12 passed, 31 deselected`.
- command: actual two-run admission/idempotence integration test
  - exit status: `0`
  - result: `1 passed in 26.27s`; both child/launcher comparisons and immutable
    replay passed with no real-root publication or rebuild execution.
- command: `uv run bandit -q -r scripts/admit_wyscout_v5_runtime.py scripts/launch_wyscout_v5.py`
  - exit status: `0`
  - result: no findings.
- command: `uv run lint-imports`
  - exit status: `0`
  - result: three contracts kept, zero broken.
- command: `uv run python scripts/verify_local_only.py`
  - exit status: `0`
  - result: PASS, 25 checks and zero failures; main branch and zero remotes.

## Artifacts/evidence

- exact admission implementation: `scripts/admit_wyscout_v5_runtime.py`
- independent retained reconstruction/rebuild controls:
  `scripts/launch_wyscout_v5.py`
- isolated attacks, actual child execution and immutable replay:
  `tests/unit/test_w04_wyscout_runtime_control.py`
- mandatory producer return:
  `reports/reviews/W04/returns/W04-WYSCOUT-RUNTIME-CONTROL-01-R3.md`

## Risks

- Runtime authority is intentionally frozen to the accepted current macOS arm64
  Python 3.12.12, uv 0.9.21, libpython and Darwin-loader bytes. Host/runtime drift
  fails closed.
- The complete required test gate passed before the final narrow strict RECORD
  path, singular association-link, exact primary-alias target, dist-info safety
  and stdlib-mode rejection hardening. Those final changes were format-, Ruff-
  and mypy-checked but, by master freeze direction, the long positive gate was
  not repeated; fresh independent review must execute it against these exact
  frozen hashes.
- The rebuild entrypoint remains authorized downstream work and is absent, so the
  strengthened rebuild executor was statically verified but not executed. Launch
  preparation still proves no rebuild occurs before manifest readback/build-ID.
- Same-trust-domain transient replacement remains a local residual; descriptor,
  no-follow, exact-byte, inventory and pre/post comparisons reject persistent or
  observed drift but are not a separate cryptographic isolation boundary.

## Follow-up items

- Fresh independent R3 review must bind the four frozen artifact hashes, rerun the
  full positive/adversarial gate, independently reconstruct all twenty component
  values/counts and specifically attack each R2 P1 counterexample.

## Scope confirmation

- no Git operations: confirmed
- no unauthorised dependency or lockfile changes: confirmed
- no edits outside `allowed_paths`: confirmed
