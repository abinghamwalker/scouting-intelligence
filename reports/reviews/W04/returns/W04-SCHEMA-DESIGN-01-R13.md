# Subagent return

## Task

- task_id: `W04-SCHEMA-DESIGN-01-R13`
- objective: Retain every R12 closure while making the local-control bootstrap
  executable through truthful pre-guard encoding-source loading, exact inherited
  source descriptors, and exhaustive typed child-result payloads.

## Files changed

- `reports/reviews/W04/wyscout-schema-design-R13.md`
- `reports/reviews/W04/returns/W04-SCHEMA-DESIGN-01-R13.md`

## Summary

- Replaced R12 in full while retaining its source, rights, strict-temporal,
  identity/correction, product/key, source/Gold coverage, quarantine,
  path/serializer, Packaging, three-`.pth`, editable-root, 35-executable,
  three-alias, resource, two-root, health/card, gate, ownership, and two-local-
  commit ledger closures.
- Retained the current repository census exactly: 58 pycs in 19 `__pycache__`
  directories, comprising 35 mapped normal, 20 mapped pytest, and the three exact
  source-absent optional inert repository orphans. Retained the site `six`
  predicate plus those three repository predicates as exactly four optional
  predicates total.
- Replaced the false built-in/frozen-only startup statement. The exact outer
  CPython 3.12.12 launch now admits and verifies pre-guard source loading for
  `encodings`, `encodings.aliases`, and `encodings.utf_8` using the exact
  independently reproduced source sizes and SHA-256 values. The design makes no
  audit-hook claim for work before hook installation.
- Added the distinct exact control prefix
  `data/working/wyscout/v5/.staging/control/control_run_id=<uuid>/runtime-pycache/`.
  The master trust-root owner alone samples and creates it, selects it before
  `uv`/Python, proves source rather than installed-pyc loading under the unchanged
  outer argv, and proves the prefix unchanged and empty after exit without
  cleanup.
- Defined exactly three ordered Python roles and three distinct prefixes while
  preserving both unchanged child argv. Prefix policy/role are stable; UUIDs,
  absolute paths, directory identities, and observations remain operational and
  excluded from stable/build identity.
- Added strict-decimal `W04_LAUNCHER_SOURCE_FD` transport for the one
  master-opened `O_RDONLY|O_NOFOLLOW` launcher source descriptor, including
  `lstat`/`fstat`, mode/link/size/bytes/digest/EOF/offset checks,
  inheritable/`FD_CLOEXEC` transition, exact-`uv` preservation test, outer
  first-instruction comparison, re-armed `FD_CLOEXEC` before child launch,
  launcher-only lifetime through both children, sole logical close ownership,
  extra-inherited-FD denial, and no path-reopen fallback.
- Added a dedicated read-only child entrypoint descriptor named by
  `W04_ENTRYPOINT_SOURCE_FD` alongside each existing result descriptor. The
  design binds path, bytes, digest, role, argv, environment, nonce, first/final
  child observation, launcher-retained observation, lifetime, and close order.
  It corrects the earlier result-descriptor-only inheritance claim.
- States exactly what persistent replacement/mutation is detected at
  pre/open/self/final/post checkpoints. It expressly retains threat-model T03/T06
  residual risk: a same-trust-domain transient path replace-and-restore timed only
  around CPython's path open may remain unobservable and is not claimed
  cryptographically prevented.
- Replaced the two prose-only role result objects with closed
  `w04-child-result-v2` schemas. The common ten-key object, fourteen-key
  descriptor observation, nine-key admission result, twenty ordered
  component-proof rows, six-key rebuild result, exact receipt row, three ordered
  layer-manifest rows, and seventeen-key final recheck all define JSON types,
  nullability, cardinality/order, path/digest/base64url/UUID grammar, and
  cross-field equality.
- Retained `W04CRSLT` framing, payload SHA-256, nonce, strict EOF, 16 MiB bound,
  separate 1 MiB diagnostics, 21,600-second monotonic deadline, and exact exit
  ordering. Canonical JSON rejects missing, unknown, duplicate, mistyped, null,
  noncanonical, unordered, inconsistent, and over-cardinality values.
- Updated stable admission/build identity to
  `w04-code-environment-admission-v10` and `w04-wyscout-build-id-v11`, carrying
  the encoding-source verifier, three-prefix policy, inherited descriptor
  policies, and exhaustive result schemas without admitting operational FD/run/
  path observations.
- Final lineage states that R13 closes the returned R12 master findings without
  self-approval.
- The design is 2,655 lines, 144,640 bytes, SHA-256
  `603178b766a1c8970fb0c215d7c987561e1aa41a5961830049e860aa051da6ac`.

## Tests run

- command: `uv run --locked --no-sync python -c "from pathlib import Path; p=Path('reports/reviews/W04/wyscout-schema-design-R13.md'); assert p.is_file() and p.stat().st_size > 26000"`
  - exit status: `0`
  - result: PASS; the exact R13 design exists and exceeds the required size.
  - note: the first managed-sandbox attempt exited `2` because `uv` could not read
    the existing user cache. The exact locked/no-sync command was rerun with read
    access and passed without sync or dependency mutation.
- command: `uv run --locked --no-sync python scripts/verify_local_only.py`
  - exit status: `0`
  - result: PASS; validator status `PASS`, failures `[]`, and all 25 reported
    checks passed.
  - note: the first managed-sandbox attempt had the same existing-cache read
    denial; the exact command passed when rerun with read access.

## Artifacts/evidence

- `reports/reviews/W04/wyscout-schema-design-R13.md`
- `reports/reviews/W04/returns/W04-SCHEMA-DESIGN-01-R13.md`
- `reports/verification/W04/wyscout-schema-design-R12-master-verification.md`
  supplies the independently reproduced three encoding source rows corrected by
  this revision.

## Risks

- The future launcher and both child entrypoints remain unavailable until bounded
  implementation packets create them and master plus independent review freeze
  their exact accepted bytes, sizes, modes, digests, and bootstrap tuples. R13
  creates no implementation file.
- The descriptor/checkpoint design detects persistent replacement and mutation at
  its named observations but intentionally does not claim to prevent an
  unobservable transient same-trust-domain replace-and-restore around CPython's
  path-based script open. This remains the explicit T03/T06 residual risk.
- If the exact `uv` path cannot preserve the inherited descriptors, or the empty
  control prefix cannot reproduce the exact source-only encoding startup with no
  prefix write under `-B`, implementation must stop rather than use a reopen
  fallback, delete pycs, or weaken denial.

## Follow-up items

- `none`

## Scope confirmation

- no Git operations: confirmed
- no unauthorised dependency or lockfile changes: confirmed
- no edits outside `allowed_paths`: confirmed
- no network/provider access, environment mutation, cleanup, delegation, or
  self-approval: confirmed
- no implementation, test, configuration, data, orchestration, migration, or
  script file was created or edited: confirmed
