# Subagent return

## Task

- task_id: `W04-SCHEMA-DESIGN-01-R16`
- objective: Retain every passing R15 and independent R9 closure while correcting
  all outer/child `UV` actual-map and digest authority to the exact normal
  PATH-resolved logical launch spelling, constructively binding its exact raw
  one-hop symlink to the already admitted uv 0.9.21 physical bytes.

## Files changed

- `reports/reviews/W04/wyscout-schema-design-R16.md`
- `reports/reviews/W04/returns/W04-SCHEMA-DESIGN-01-R16.md`

## Summary

- Carried the complete R15 standalone design forward without compressing or
  weakening its source, rights, temporal, identity, product, coverage,
  serializer, path, quarantine, ownership, gate, two-root, or two-commit ledger
  contracts.
- Retained the R15 acyclic exact 25-key pre-build projection and post-hash
  25-key rebuild invocation, exact five-field `EvidenceDependency` schema and
  five-row chronology, 16/8/10 child input envelopes, 25-key environment/result
  authority, three unchanged visible argv and process roles, 35 executables,
  three interpreter aliases, three denied `.pth` classes, source-complete
  58-pyc/19-cache-directory repository authority plus four optional predicates,
  and the exact 17-resource closure.
- Corrected the outer and both child uv-input maps and actual Python maps to use
  exactly `UV="/opt/homebrew/bin/uv"`. Normal `PATH` resolution of the one visible
  literal `uv` token must select only that logical path. The admitted uv 0.9.21
  transformation reproduces the same exact logical spelling while changing only
  the declared recursion depth and venv `PATH` prefix. A single stable
  `<W04_UV_LOGICAL_LAUNCH_PATH>` substitution now covers every outer/child
  normalized base digest; the former physical-path token is absent.
- Added constructive uv launch authority: `/opt/homebrew/bin/uv` must be exactly
  one symlink with raw target `../Cellar/uv/0.9.21/bin/uv` (26 bytes), resolving
  in exactly one hop contained within `/opt/homebrew` to the regular
  `/opt/homebrew/Cellar/uv/0.9.21/bin/uv`. The final file remains mode `0o555`,
  41,617,552 bytes, SHA-256
  `4f0c0c002bb4702c1bd6792edc15f7ae3948b5f19509c8d73cd5c9a26298097f`,
  with version `uv 0.9.21 (Homebrew 2025-12-30)`.
- Updated the bootstrap tuple, exact input-to-Python transforms, acyclic
  fixed/base/complete environment digests, child envelopes and comparisons,
  stable/operational truth split, code/environment manifest components, build
  projection binding, health evidence, two-root proof, and positive/negative
  implementation tests consistently. Stable algorithm identifiers advance to
  `w04-local-control-bootstrap-v3`,
  `w04-outer-environment-bootstrap-v2`,
  `w04-child-environment-input-v2`, and
  `w04-code-environment-admission-v13`.
- Explicitly rejects a missing or non-symlink logical entry, raw-link drift,
  extra hop, cycle, escape, alternate logical path, direct physical exec-target
  launch, accepting either spelling, post-hoc realpath normalization, physical
  byte/version/mode/size drift, unknown environment values, input/first/final
  disagreement, and outer/child/sibling-child disagreement.
- Preserved the recorded R15 master-candidate and independent R9 `REWORK`
  decisions without self-approval. This packet names but does not create any
  future implementation script.

## Tests run

- command:
  `uv run --locked --no-sync python -c "from pathlib import Path; p=Path('reports/reviews/W04/wyscout-schema-design-R16.md'); assert p.is_file() and p.stat().st_size > 26000"`
  - exit status: `0`
  - result: PASS; the standalone R16 design exists and is 185,625 bytes.
- command: `uv run --locked --no-sync python scripts/verify_local_only.py`
  - exit status: `0`
  - result: PASS; validator status `PASS`, all 25 checks passed, and
    `failures: []`. Its Git checks were read-only.
- command:
  `ls -ld /opt/homebrew/bin/uv /opt/homebrew/Cellar/uv/0.9.21/bin/uv`;
  `readlink /opt/homebrew/bin/uv`;
  `stat -f '%N|%HT|%p|%z' ...`;
  `shasum -a 256 /opt/homebrew/Cellar/uv/0.9.21/bin/uv`;
  `/opt/homebrew/bin/uv --version`
  - exit status: `0`
  - result: PASS; reproduced the exact symlink type/raw target/26-byte length,
    physical regular-file type/mode/size/digest, and version recorded above.
- command:
  `find scripts -maxdepth 1 -type f \( -name 'launch_wyscout_v5.py' -o -name 'admit_wyscout_v5_runtime.py' -o -name 'rebuild_wyscout_v5.py' \) -print`
  - exit status: `0`
  - result: PASS; output was empty, so all three future scripts remain absent.

## Artifacts/evidence

- `reports/reviews/W04/wyscout-schema-design-R16.md`
- `reports/reviews/W04/returns/W04-SCHEMA-DESIGN-01-R16.md`
- sole normalized logical launch token:
  `<W04_UV_LOGICAL_LAUNCH_PATH>`
- exact logical launch authority: `/opt/homebrew/bin/uv`
- exact raw symlink target: `../Cellar/uv/0.9.21/bin/uv`
- exact physical byte authority:
  `/opt/homebrew/Cellar/uv/0.9.21/bin/uv`
- admitted code/environment schema:
  `w04-code-environment-admission-v13`

## Risks

- This is design evidence only. The three future scripts and their tests remain
  absent; exact launch behavior still requires implementation by the named later
  owners plus master and independent review.
- Runtime device, inode, link-count, and clock observations remain operational.
  Implementers must separately reproduce those observations while requiring the
  stable logical path, raw target, one-hop relationship, and physical byte
  authority exactly; no aggregate digest permits skipping constructive checks.
- The initial unprivileged size probe could not read the existing user uv cache
  because of workspace sandboxing. It made no project mutation; the exact packet
  command was rerun with approved read access and passed.
- The same-trust-domain transient replacement residual retained from R15 remains
  explicit and unchanged; R16 does not claim an unavailable stronger
  observation.

## Follow-up items

- Master and independent reviewer should reproduce the exact logical
  PATH-selection/`UV` maps, one-hop symlink proof, sole-token digest construction,
  stable/operational split, two-root equality, and retained R15/R9 closures before
  accepting or dispatching implementation.

## Scope confirmation

- no Git operations: confirmed; no Git command or `.git/**` mutation was
  performed. The mandated local-only verifier performed only its documented
  read-only Git checks.
- no unauthorised dependency or lockfile changes: confirmed; neither
  `pyproject.toml` nor `uv.lock` was modified, and checks used
  `--locked --no-sync`.
- no edits outside `allowed_paths`: confirmed; exactly the two R16 report paths
  listed above were created/modified. No orchestration path, parent-workspace
  report, implementation file, dependency, provider/network resource, or cleanup
  obligation was created.
