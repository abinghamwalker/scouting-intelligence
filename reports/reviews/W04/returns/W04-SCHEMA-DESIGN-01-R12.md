# Subagent return

## Task

- task_id: `W04-SCHEMA-DESIGN-01-R12`
- objective: Retain R11 in full while closing the returned independent R8
  repository-bytecode census/orphan findings and the undefined external-launcher
  authority.

## Files changed

- `reports/reviews/W04/wyscout-schema-design-R12.md`
- `reports/reviews/W04/returns/W04-SCHEMA-DESIGN-01-R12.md`

## Summary

- Replaced R11 in full without compressing or substituting its source, rights,
  strict-temporal, identity/correction, key, source/Gold coverage, path/serializer,
  Packaging, three-`.pth`, editable-root, 35-executable, three-alias,
  source-complete-pyc, resource, build/two-root, health/card, gate, ownership, or
  two-local-commit contracts.
- Corrected the whole-repository operational census to exactly 58 pycs in 19
  `__pycache__` directories: 35 mapped normal, 20 mapped pytest, and three exact
  source-absent optional inert orphans.
- Retained the exact PostgreSQL orphan predicate and added exact denial-only
  predicates for `migrations/__pycache__/env.cpython-312.pyc` and
  `migrations/versions/__pycache__/0001_foundation.cpython-312.pyc`, including
  absent Python siblings, magic, tag, mode, size, and SHA-256. The existing SQL
  file grants no Python authority.
- Propagated all four optional-orphan predicates—the site `six` predicate plus the
  three repository predicates—through stable/operational evidence, health,
  negative tests, required tests, and two-root equality. Presence and operational
  bytes remain excluded from stable identity; each optional file may be absent in
  another root.
- Named the exact future repository-owned transient local control entry point
  `scripts/launch_wyscout_v5.py` without creating it and bound its exact invocation
  to `uv run --locked --no-sync python -S -B scripts/launch_wyscout_v5.py` from the
  repository root under role `W04_LOCAL_CONTROL`.
- Defined its non-circular pre-execution bootstrap tuple over uv, the physical
  Python interpreter, launcher path/bytes/mode/size/digest, ordered argv, fixed
  environment, and project/lock digests, followed by inclusion of the verified
  launcher row in stable code/environment and build identity.
- Assigned the launcher sole runtime ownership for both UUIDs/prefixes, child
  environment construction, whole-repository/site pyc snapshots, both exact child
  launches, result handling, immutable code-manifest publication, build-ID
  calculation, and final empty/no-read/no-change checks. It has no product,
  layer-manifest, boundary-receipt, or rebuild-receipt write authority.
- Preserved both exact child argv unchanged and specified a fresh anonymous
  one-way result pipe per child, descriptor close rules, `W04CRSLT` version-1
  length framing, 16 MiB maximum, canonical closed JSON payload, nonce/role/code
  bindings, payload SHA-256, strict EOF and exit ordering, a 21,600-second
  monotonic deadline, and separate bounded stdout/stderr diagnostics.
- Closed launcher and child entry-point replacement/mutation races with contained
  no-follow regular-file checks, open descriptors, identity/mode/size/digest
  binding, child self-observation, and fail-closed pre/post rechecks.
- Updated stable algorithm labels to `w04-preexisting-pyc-enumerate-deny-v4`,
  `w04-code-environment-admission-v9`, and `w04-wyscout-build-id-v10`.
- Corrected final lineage exactly: R12 closes the returned independent R8 findings
  without self-approval.
- The design is 2,379 lines, 127,477 bytes, SHA-256
  `f265b0761d8d7830b7c75d7b45e6d156837b8555f0d9c269df96f3a074306fd5`.

## Tests run

- command: `uv run --locked --no-sync python -c "from pathlib import Path; p=Path('reports/reviews/W04/wyscout-schema-design-R12.md'); assert p.is_file() and p.stat().st_size > 24000"`
  - exit status: `0`
  - result: PASS; the exact R12 design exists and exceeds the required size.
  - note: the first managed-sandbox attempt exited `2` because uv could not read
    the existing user cache; the exact command was rerun with read access and
    passed without sync or mutation.
- command: `uv run --locked --no-sync python scripts/verify_local_only.py`
  - exit status: `0`
  - result: PASS; validator status `PASS`, failures `[]`, and all 25 reported
    checks passed.

## Artifacts/evidence

- `reports/reviews/W04/wyscout-schema-design-R12.md`
- `reports/reviews/W04/returns/W04-SCHEMA-DESIGN-01-R12.md`

## Risks

- No residual design, scope, dependency, environment, architecture, provider,
  network, or local-only conflict found.
- The future launcher and both child entry points remain unavailable until their
  implementation packets create them and master plus independent review freeze
  their exact accepted bytes. R12 intentionally creates no implementation file.

## Follow-up items

- `none`

## Scope confirmation

- no direct Git operation, commit, tag, or mutation: confirmed; the mandated
  verifier performed only its own read-only Git checks
- no unauthorized dependency, lockfile, environment, or configuration change:
  confirmed
- no edits outside `allowed_paths`: confirmed
- no network/provider access, cleanup, delegation, or self-approval: confirmed
- `scripts/launch_wyscout_v5.py`,
  `scripts/admit_wyscout_v5_runtime.py`, and
  `scripts/rebuild_wyscout_v5.py` were not created or edited: confirmed
