# Subagent return

## Task

- task_id: `W09-FULL-CANONICAL-BUILD-02B`
- revision: `R3`
- objective: Normalize the retained optional `subEventId` empty-string sentinel at
  the provider-adapter boundary and prove a full no-write canonical action traversal.

## Status

- recommendation: **BOUNDED FIX COMPLETE; PACKET STOP CONDITION TRIGGERED**
- The exact `subEventId` defect is resolved and its full retained population is
  reconciled.
- The required full canonical projection traversal cannot yet complete because it
  exposed a separate retained coordinate-domain conflict. The R3 packet explicitly
  requires a stop on any other source value/type conflict, so this revision does not
  broaden the implementation to interpret those coordinates.

## Files changed

- `src/scouting/sources/wyscout_historical.py`
- `src/scouting/data_products/wyscout/historical.py`
- `tests/unit/test_w09_wyscout_historical_adapter.py`
- `tests/integration/test_w09_full_canonical_build.py`
- `reports/reviews/W09/returns/W09-FULL-CANONICAL-BUILD-02B-R3.md`

## Implemented correction

- Added one narrow adapter projection for optional `subEventId`:
  - raw JSON integers remain strict integers;
  - raw null remains null;
  - only the exact raw empty string is normalized to null and visibly marked as the
    admitted sentinel;
  - quoted numerics, whitespace, booleans, floats and other strings fail closed.
- The retained DuckDB JSON logical token is decoded before strict type checking, so a
  numeric JSON token remains distinguishable from a quoted numeric string. Explicit
  fixtures pass through the same int-or-null semantic boundary.
- `strict_int` is unchanged; the canonical producer performs no generic
  string-to-integer coercion.
- Every retained partition traversal now reconciles its exact empty-sentinel count
  before the adapter releases completion:
  - England: 1,558
  - France: 1,543
  - Germany: 1,219
  - Italy: 1,620
  - Spain: 1,881
  - total: 7,821
- The canonical manifest now records the adapter contract, normalization rule, total
  sentinel count and deterministic per-partition counts. Retained builds fail closed
  if these counts drift.
- Added `audit_historical_action_projection`, a no-write path through the exact
  canonical action projection. It retains strict clocks, identities, action
  predicates and the adapter's before/after action-file fingerprints.

## Checks run

- `uv run ruff format --check src/scouting/sources/wyscout_historical.py src/scouting/data_products/wyscout/historical.py scripts/build_w09_historical_canonical.py tests/unit/test_w09_wyscout_historical_adapter.py tests/integration/test_w09_full_canonical_build.py`
  - exit status: `0`; all five files formatted.
- `uv run ruff check ...`
  - exit status: `0`; all checks passed.
- `uv run mypy src/scouting/sources/wyscout_historical.py src/scouting/data_products/wyscout/historical.py scripts/build_w09_historical_canonical.py`
  - exit status: `0`; no issues in three source files.
- `uv run pytest -q tests/unit/test_w09_wyscout_historical_adapter.py tests/integration/test_w09_full_canonical_build.py`
  - exit status: `0`; 24 tests passed. Adversarial coverage includes exact empty
    sentinel, integer, quoted numeric, whitespace, Boolean, float and other string.
- `.venv/bin/bandit -q -r src/scouting/sources/wyscout_historical.py src/scouting/data_products/wyscout/historical.py scripts/build_w09_historical_canonical.py`
  - exit status: `0`; no security findings. The exact `uv run bandit` wrapper was
    blocked only by sandbox access to the existing external uv cache, so the same
    environment executable was invoked directly.
- Caffeinated retained adapter-only traversal across all five exact action payloads:
  - exit status: `0`;
  - output: `3071395 7821 {'England': 1558, 'France': 1543, 'Germany': 1219, 'Italy': 1620, 'Spain': 1881}`;
  - all action fingerprints remained stable.
- Caffeinated retained no-write canonical action projection:
  - exit status: `1` at the required stop condition;
  - England and France completed after exact sentinel reconciliation; Germany then
    failed with `HistoricalCanonicalBuildError: action coordinates are outside 0..100`.

## Newly exposed retained conflict

A bounded read-only DuckDB reconciliation found exactly three out-of-domain coordinate
points on three actions:

- Germany action `225765702`, match `2516920`, player `15090`:
  `[{y: 50, x: 14}, {y: 101, x: 28}]`.
- Germany action `225765704`, match `2516920`, player `32959`:
  `[{y: 101, x: 28}, {y: 95, x: 18}]`.
- Italy action `198907641`, match `2576042`, player `166534`:
  `[{y: 47, x: -1}, {y: 12, x: 69}]`.

Per-partition out-of-domain point counts were England 0, France 0, Germany 2,
Italy 1 and Spain 0. No source bytes were changed and no canonical production artifact
was generated by this packet.

## Required follow-up

- The master must issue a separate bounded decision/rework packet for the three raw
  coordinate sentinels or anomalies. Until that policy is accepted, the canonical
  build and dependent feature-matrix packet remain correctly blocked.
- After that bounded correction, rerun this same no-write full canonical traversal,
  then let the master materialize the final production artifacts.

## Scope confirmation

- no Git operations: confirmed.
- no dependency, lockfile, contract or orchestration edits: confirmed.
- no edits outside the R3 allowed paths: confirmed.
- no network/provider access and no final production artifact generation: confirmed.
